---
fileType: {{ 'tsx' if inputs.useTypescript else 'jsx' }}
---
{% if inputs.useTypescript -%}
export default function {{ title | replace(' ', '') | capitalize }}Page(): JSX.Element {
{%- else -%}
export default function {{ title | replace(' ', '') | capitalize }}Page() {
{%- endif %}
  return (
    <div className="container mx-auto px-4 py-8">
      <h1 className="text-3xl font-bold mb-6">{{ title }}</h1>
      <p className="text-gray-600">
        This is the {{ title }} page. Add your content here.
      </p>

      <div className="mt-8">
        <h2 className="text-xl font-semibold mb-4">Getting Started</h2>
        <p className="text-gray-700">
          Edit this file at{' '}
          <code className="bg-gray-100 px-2 py-1 rounded text-sm">
            {{ 'app' if inputs.appRouter else 'pages' }}{{ route }}/page.{{ 'tsx' if inputs.useTypescript else 'jsx' }}
          </code>
        </p>
      </div>
    </div>
  );
}