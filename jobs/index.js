/**
 * @fileoverview Bree Jobs Index
 *
 * This file serves as the entry point for Bree job discovery.
 * Bree dynamically loads job files from the jobs directory.
 *
 * Job files should be ES modules (.mjs) that export a default function
 * that Bree will execute as a worker.
 *
 * @version 1.0.0
 * @license Apache-2.0
 */

export default [];
